DATABASE_URL="mongodb+srv://USER:PASSWORD@cluster0.ps0qux7.mongodb.net/influconnect?retryWrites=true&w=majority"
ADMIN_PASSWORD="changeme"
# JWT signing secret for brand/creator sessions (use a long random string in production)
SESSION_SECRET="change-me-to-a-long-random-string"
# Optional: Instagram sessionid cookie for reliable scraping (instagram.com → DevTools → Application → Cookies)
INSTAGRAM_SESSION_ID=""

# Email (EmailJS) — https://www.emailjs.com
# Enable "Use Private Key" under Account → Security
EMAILJS_SERVICE_ID=""
EMAILJS_TEMPLATE_ID=""
EMAILJS_PUBLIC_KEY=""
EMAILJS_PRIVATE_KEY=""
# Public site URL for links in emails (Netlify sets URL automatically if omitted)
APP_URL="http://localhost:3000"
