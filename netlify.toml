# Next.js App Router — requires @netlify/plugin-nextjs (see package.json)
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  external_node_modules = ["mongodb"]
